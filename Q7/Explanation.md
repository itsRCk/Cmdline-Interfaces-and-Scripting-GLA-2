# Text-Pattern Sorting

The script validates the input file and converts all text to lowercase to ensure case-sensitivity matching.
Each word is extracted and checked using regular expressions to classify it as vowel-only, consonant-only, or mixed starting with a consonant. The results are written into separate output files(`vowels.txt`, `consonants.txt`, `mixed.txt`) accordingly.